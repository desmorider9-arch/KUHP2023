source "https://rubygems.org"

# Pin fastlane so CI uses a reproducible version.
# Adjust versions if you have specific constraints.
gem "fastlane", "2.228.0"
# Optional: pin CocoaPods if you want to install it via Bundler in CI
gem "cocoapods", "~> 1.12"
